@{
    ViewBag.Title = "Home";
}


@model IEnumerable<Assemblify.Web.ViewModels.Post.UserPostsViewModel>

<div class="row">
    @foreach (var post in Model)
    {
        @Html.Partial("_PostUserPostDetailsPartial", post);
    }
</div>